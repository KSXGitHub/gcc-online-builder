dependencies:
    pre:
        - sudo apt-get install gcc-multilib g++-multilib
        - ./build.sh:
            timeout: 1800
deployment:
    staging:
        branch:
            - master
            - test
general:
    artifacts:
        - "object/out"
        - "logs"
